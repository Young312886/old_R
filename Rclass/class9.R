Apps <- read.csv("07192013Apps.csv")
summary(Apps[,c('Source','Collection Date','')])
##combine , cleansing and normalize##
#present correlation# Interpret by two things - direction, number
#use ln([,data=Apps)#
#how to improve Rsquare = more independent value or increase number of data, reselecting value
